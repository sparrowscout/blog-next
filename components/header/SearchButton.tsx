import IconButton from '../common/IconButton'
import SearchIcon from '@/assets/icons/search.svg'

export default function SearchButton() {
  return (
    <IconButton>
      <SearchIcon style={{ fill: '#FF01FF' }} />
    </IconButton>
  )
}
